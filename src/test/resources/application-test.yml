spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      # cria o schema do banco no início dos testes e o destrói no final.
      ddl-auto: create-drop
