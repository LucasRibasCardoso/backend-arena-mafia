spring:

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

  security:
    jwt:
      secret-key: 551VM4LcN284uYnv3MjDckhe9IQ6ta7/0jNwrvNqRz0=
      expirationMs: 3600000 # 1 hour
      issuer: "arena-mafia-api"
